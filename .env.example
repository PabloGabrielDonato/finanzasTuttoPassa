PORT=3000
JWT_SECRET=tuttopassa_secret_key_2026

# Conectividad SQL
# Para MySQL (Hostinger u local) completar los siguientes campos.
# Si se dejan en blanco o fallan, el servidor iniciará automáticamente con SQLite local (database.sqlite) para fácil desarrollo.
DB_HOST=mysql
DB_USER=
DB_PASSWORD=
DB_NAME=
DB_PORT=3306
